It is just a simple demo with fixed songs inside the app to choose from ... if you wanna to add more songs,just use internet source instead of local source in case the app is too big too be downloaded

The 12.16 updated file is a Hap that can ONLY be used on local emulators. The AVplayer would be malfunctioning if you use the remote emulator.

This file is a Harmony OS app ,if u want it to run on Openharmony ,please replace the build-file.json5 in the main directory with the file here.
